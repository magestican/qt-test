const request = {
  /*
   * Performs a GET request and returns a RequestReturnObject
   * request.get(url: String): RequestReturnObject
   */
  get : (url)=>{} //can i wrap get as a promise? or use callbacks?
}


module.exports = request;
